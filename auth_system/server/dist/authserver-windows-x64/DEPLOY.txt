部署说明

1. 将 .env.example 复制为 .env，或者直接修改包内自带的 .env
2. 至少修改以下配置：
   - ADMIN_USERNAME
   - ADMIN_PASSWORD
   - ADMIN_SESSION_SECRET
3. 如果要开放后台到公网：
   - ADMIN_ALLOW_PUBLIC=true
4. 当前包已包含 SQLite 数据库：data\auth_system.db
5. 双击 start_authserver.bat 或直接运行 authserver.exe
6. 默认端口：8080，请在服务器防火墙放行
